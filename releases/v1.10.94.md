# IICP PHP Directory v1.10.94

This candidate bounds the package runtime claim to PHP 8.3 and serializes
restricted-domain membership creation and rotation per subject. Public-mode
defaults, API and wire representations remain unchanged.

Preparing this source does not publish or deploy it, change Genesis authority,
enable restricted mode or authorize federation.
