openapi: "3.1.0"
info:
  title: IICP Directory Service
  version: "1.6.0"
  description: >
    IICP directory service — node registration, heartbeat, discovery, and
    node detail. Traffic NEVER routes through the directory; it is
    bootstrap and discovery only. The IICP specification is normative;
    this OpenAPI document is a machine-readable projection of the supported
    HTTP contract and must not silently override the specification. The
    info.version value versions this documented OpenAPI contract; runtime,
    schema, deployment, and historical parity-evidence versions are tracked
    separately.
  contact:
    url: https://iicp.network
  license:
    name: Apache 2.0
    identifier: Apache-2.0

servers:
  - url: https://iicp.network/api/v1
    description: Production

tags:
  - name: registration
    description: Node self-registration and liveness
  - name: discovery
    description: Client node discovery
  - name: operator
    description: Operator-key authenticated self-service

paths:
  /register:
    post:
      operationId: registerNode
      tags: [registration]
      summary: Register a new node
      description: >
        Called by a provider node at startup. Returns a node_token that
        MUST be used on all subsequent heartbeat calls. Rate-limited to
        60 requests per minute per IP in production.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/NodeRegistration"
            example:
              endpoint: "https://node.example.com"
              region: "eu-central"
              capabilities:
                - intent: "urn:iicp:intent:llm:chat:v1"
                  models: ["llama3"]
                  max_tokens: 8192
              limits:
                max_concurrent: 4
                tokens_per_min: 20000
      responses:
        "201":
          description: Registration successful
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RegistrationResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "422":
          $ref: "#/components/responses/ValidationError"
        "429":
          $ref: "#/components/responses/RateLimited"

  /heartbeat:
    post:
      operationId: nodeHeartbeat
      tags: [registration]
      summary: Update node liveness
      description: >
        Called by a registered node every 30 seconds. Nodes not seen
        within 90 seconds are marked unavailable and excluded from
        discovery results. Rate-limited to 60 requests per minute per node.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/HeartbeatRequest"
            example:
              load: 0.3
              active_jobs: 1
      responses:
        "200":
          description: Heartbeat accepted
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                    enum: [ok]
        "401":
          $ref: "#/components/responses/Unauthorized"
        "429":
          $ref: "#/components/responses/RateLimited"

  /discover:
    get:
      operationId: discoverNodes
      tags: [discovery]
      summary: Discover eligible nodes for an intent
      description: >
        Returns nodes pre-scored and pre-sorted by the directory using
        the ADR-008 composite formula. Nodes are filtered by availability
        window and last_seen within 90 seconds. Score < 0.1 nodes are
        excluded. Clients MUST NOT re-score results. Default responses are
        route-dispatch data and include node_id + serving endpoint fields so
        clients can submit tasks directly to the selected node. Use
        view=public for a presentation-safe response that redacts full node IDs,
        endpoint URLs, native transport URLs, opaque transport metadata and raw
        CX key material.
      parameters:
        - name: intent
          in: query
          required: true
          schema:
            type: string
            pattern: "^urn:iicp:intent:(?:[a-z][a-z0-9_]*|x\\.[a-z][a-z0-9-]*):[a-z][a-z0-9_-]*:v[1-9][0-9]*$"
          example: "urn:iicp:intent:llm:chat:v1"
        - name: qos
          in: query
          required: false
          schema:
            type: string
            enum: [realtime, interactive, batch, best-effort]
            default: interactive
        - name: region
          in: query
          required: false
          schema:
            type: string
          example: "eu-central"
        - name: limit
          in: query
          required: false
          schema:
            type: integer
            minimum: 1
            maximum: 50
            default: 10
        - name: view
          in: query
          required: false
          schema:
            type: string
            enum: [dispatch, public]
            default: dispatch
          description: >
            dispatch returns route-bearing data for task submission. public
            returns a presentation-safe projection without full node IDs,
            endpoint URLs, native transport URLs, transport metadata or raw CX
            public keys.
      responses:
        "200":
          description: Scored node list
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DiscoveryResponse"
        "422":
          $ref: "#/components/responses/ValidationError"
        "429":
          $ref: "#/components/responses/RateLimited"

  /dispatch/ticket:
    post:
      operationId: issueDispatchRouteTicket
      tags: [discovery]
      summary: Request one short-lived route-bearing dispatch ticket
      description: >
        Control-plane-only route selection. Task prompts and payloads are
        rejected. Clients may exclude previously failed node ID prefixes when
        requesting the next candidate. Full tickets and route URLs must not be
        logged.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [intent]
              properties:
                intent: { type: string }
                qos: { type: string, enum: [realtime, interactive, batch, best-effort] }
                region: { type: string }
                model: { type: [string, "null"], maxLength: 128 }
                min_reputation: { type: number, minimum: 0, maximum: 1 }
                limit: { type: integer, minimum: 1, maximum: 50 }
                max_multiplier: { type: number, minimum: 0 }
                min_quality_score: { type: number, minimum: 0, maximum: 1 }
                cip_capable: { type: boolean }
                relay_capable: { type: boolean }
                include_internal: { type: boolean }
                modality: { type: string, enum: [text, image, audio, video] }
                score_version: { type: string, enum: [v2_shadow] }
                node_id: { type: string }
                node_id_prefix: { type: string, minLength: 4, maxLength: 36 }
                exclude_node_id_prefixes:
                  type: array
                  maxItems: 10
                  uniqueItems: true
                  items: { type: string, minLength: 4, maxLength: 36 }
      responses:
        "201":
          description: Ticketed concrete route; Cache-Control is no-store
        "404":
          description: No eligible route
        "409":
          description: Ambiguous node prefix
        "422":
          $ref: "#/components/responses/BadRequest"
        "503":
          description: Directory ticket signing is not configured
        "429":
          $ref: "#/components/responses/RateLimited"

  /operator/challenge:
    post:
      operationId: createOperatorChallenge
      tags: [operator]
      summary: Create a one-time operator-key challenge
      description: >
        Returns a five-minute single-use nonce for a known operator. The
        response contains only a short fingerprint and current acceptance
        versions; it never exposes the stored operator key or private records.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [operator_pub]
              additionalProperties: false
              properties:
                operator_pub: { type: string, maxLength: 64 }
      responses:
        "200": { description: One-time challenge; Cache-Control is no-store }
        "404": { $ref: "#/components/responses/NotFound" }
        "429": { $ref: "#/components/responses/RateLimited" }

  /operator/acceptance:
    post:
      operationId: acceptOperatorTerms
      tags: [operator]
      summary: Record current terms and DPA acceptance
      description: >
        Requires an Ed25519 signature over the canonical operator self-service
        envelope and a fresh challenge. Acceptance is evidence of key control
        and version acknowledgement, not legal certification.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              allOf:
                - $ref: "#/components/schemas/OperatorSignedRequest"
                - type: object
                  required: [terms_version, dpa_version]
                  properties:
                    terms_version: { type: string, maxLength: 64 }
                    dpa_version: { type: string, maxLength: 64 }
      responses:
        "200": { description: Redacted acceptance receipt; Cache-Control is no-store }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "409": { description: Acceptance version is no longer current }
        "429": { $ref: "#/components/responses/RateLimited" }

  /operator/dsr/export:
    post:
      operationId: exportOperatorData
      tags: [operator]
      summary: Export operator-linked records
      description: >
        Operator-key authenticated self-service. The authenticated operator
        key is always the selector. Results are no-store and redacted.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              allOf:
                - $ref: "#/components/schemas/OperatorSignedRequest"
                - type: object
                  properties:
                    tracking_id: { type: string, maxLength: 64 }
                    confirm: { type: boolean }
      responses:
        "200": { description: Redacted DSR result; Cache-Control is no-store }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "422": { $ref: "#/components/responses/ValidationError" }
        "429": { $ref: "#/components/responses/RateLimited" }

  /operator/dsr/restrict:
    post:
      operationId: restrictOperatorData
      tags: [operator]
      summary: Restrict operator-linked records
      requestBody:
        required: true
        content:
          application/json:
            schema:
              allOf:
                - $ref: "#/components/schemas/OperatorSignedRequest"
                - type: object
                  required: [confirm]
                  properties:
                    tracking_id: { type: string, maxLength: 64 }
                    confirm: { type: boolean, const: true }
      responses:
        "200": { description: Redacted DSR result; Cache-Control is no-store }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "422": { $ref: "#/components/responses/ValidationError" }
        "429": { $ref: "#/components/responses/RateLimited" }

  /operator/dsr/anonymize:
    post:
      operationId: anonymizeOperatorData
      tags: [operator]
      summary: Anonymize operator-linked records
      requestBody:
        required: true
        content:
          application/json:
            schema:
              allOf:
                - $ref: "#/components/schemas/OperatorSignedRequest"
                - type: object
                  required: [confirm]
                  properties:
                    tracking_id: { type: string, maxLength: 64 }
                    confirm: { type: boolean, const: true }
      responses:
        "200": { description: Redacted DSR result; Cache-Control is no-store }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "422": { $ref: "#/components/responses/ValidationError" }
        "429": { $ref: "#/components/responses/RateLimited" }

  /node/{id}:
    get:
      operationId: getNode
      tags: [discovery]
      summary: Get full node detail
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            pattern: "^[a-zA-Z0-9][a-zA-Z0-9._:-]{0,35}$"
      responses:
        "200":
          description: Node detail
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/NodeDetail"
        "404":
          $ref: "#/components/responses/NotFound"
        "429":
          $ref: "#/components/responses/RateLimited"

components:
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
      description: node_token issued at registration (Phase 1). JWT in Phase 2+.

  schemas:
    OperatorSignedRequest:
      type: object
      required: [operator_pub, nonce, ts, sig]
      properties:
        operator_pub: { type: string, maxLength: 64 }
        nonce: { type: string, minLength: 16, maxLength: 64 }
        ts: { type: integer, description: Unix timestamp within plus or minus 300 seconds }
        sig: { type: string, maxLength: 128, description: Base64 Ed25519 signature }
    NodeRegistration:
      type: object
      required: [endpoint, region, capabilities, limits]
      properties:
        node_id:
          type: string
          pattern: "^[a-zA-Z0-9][a-zA-Z0-9._:-]{0,35}$"
        endpoint:
          type: string
          format: uri
          description: Routable HTTP or HTTPS base URL where this node accepts tasks.
        transport_endpoint:
          type: [string, "null"]
          format: uri
          description: Optional routable iicp or iicpsec native transport URL.
        region:
          type: string
          description: Geographic region hint (e.g. eu-central, us-east). Required for Phase 1 discovery routing.
        public_key:
          type: string
          description: Base64-encoded public key (Phase 2+ signing).
        capabilities:
          type: array
          minItems: 1
          items:
            $ref: "#/components/schemas/Capability"
        limits:
          $ref: "#/components/schemas/NodeLimits"
        availability:
          type: array
          items:
            $ref: "#/components/schemas/AvailabilityWindow"

    Capability:
      type: object
      required: [intent, models, max_tokens]
      properties:
        intent:
          type: string
          pattern: "^urn:iicp:intent:(?:[a-z][a-z0-9_]*|x\\.[a-z][a-z0-9-]*):[a-z][a-z0-9_-]*:v[1-9][0-9]*$"
        models:
          type: array
          minItems: 1
          items:
            type: string
        max_tokens:
          type: integer
          minimum: 1

    NodeLimits:
      type: object
      additionalProperties: false
      properties:
        max_concurrent:
          type: integer
          minimum: 1
          maximum: 256
        tokens_per_min:
          type: integer
          minimum: 1

    AvailabilityWindow:
      type: object
      required: [start, end, share]
      additionalProperties: false
      properties:
        start:
          type: string
          pattern: "^([01][0-9]|2[0-3]):[0-5][0-9]$"
        end:
          type: string
          pattern: "^([01][0-9]|2[0-3]):[0-5][0-9]$"
        share:
          type: number
          minimum: 0.0
          maximum: 1.0

    RegistrationResponse:
      type: object
      required: [node_id, node_token]
      properties:
        node_id:
          type: string
          pattern: "^[a-zA-Z0-9][a-zA-Z0-9._:-]{0,35}$"
        node_token:
          type: string
          minLength: 32
          description: Bearer token for heartbeat authentication. Store securely.

    HeartbeatRequest:
      type: object
      properties:
        load:
          type: number
          minimum: 0.0
          maximum: 1.0
          description: Current load as fraction of max_concurrent.
        active_jobs:
          type: integer
          minimum: 0

    DiscoveryResponse:
      type: object
      required:
        - nodes
        - count
        - relay_available
        - query_ms
        - view
        - data_class
        - route_fields_present
      properties:
        nodes:
          type: array
          items:
            type: object
            additionalProperties: true
        count: { type: integer, minimum: 0 }
        relay_available: { type: boolean }
        query_ms: { type: number, minimum: 0 }
        view: { type: string, enum: [dispatch, public] }
        data_class: { type: string, enum: [route_dispatch, public_presentation] }
        route_fields_present: { type: boolean }
        dispatch_ticket_endpoint: { type: string }
        ticketed_dispatch_available: { type: boolean }

    NodeDetail:
      type: object
      required: [node_id, endpoint, score, available]
      properties:
        node_id:
          type: string
          pattern: "^[a-zA-Z0-9][a-zA-Z0-9._:-]{0,35}$"
        endpoint:
          type: string
          format: uri
        score:
          type: number
          minimum: 0.0
          maximum: 1.0
        available:
          type: boolean
        region:
          type: string
        capabilities:
          type: array
          items:
            $ref: "#/components/schemas/Capability"
        last_seen:
          type: string
          format: date-time

    Error:
      type: object
      required: [error]
      properties:
        error:
          type: object
          required: [code, message]
          properties:
            code:
              type: string
            message:
              type: string
            details:
              type: object

  responses:
    BadRequest:
      description: Invalid request parameters
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/Error"
    Unauthorized:
      description: Missing or invalid node_token
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/Error"
    NotFound:
      description: Node not found
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/Error"
    ValidationError:
      description: Semantic validation failure
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/Error"
    RateLimited:
      description: Rate limit exceeded
      headers:
        X-RateLimit-Remaining:
          schema:
            type: integer
        Retry-After:
          schema:
            type: integer
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/Error"
