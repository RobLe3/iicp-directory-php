* text=auto eol=lf

*.blade.php diff=html
*.css diff=css
*.html diff=html
*.md diff=markdown
*.php diff=php

# The dedicated PHP repository is exported with `git archive` by the mirror
# synchronizer. Keep its CI workflow in that export so parity checks remain
# active after every seed update.
CHANGELOG.md export-ignore
.styleci.yml export-ignore
